<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <style>
    body { background-color: black; } form {text-align: center; background-color: #f8f9fa; padding: 20px; border-radius: 8px; box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); max-width: 400px; margin: 25vh auto; } form label { color: #28394d; font-weight: bold; } form input { width: calc(100% - 20px); padding: 10px; margin: 8px 0; border: 1px solid #ced4da; border-radius: 4px; font-size: 16px; } button {display: flex; margin: auto; background-color: #007bff; color: white; border: none; padding: 12px
    20px; text-align: center; border-radius: 4px; cursor: pointer; transition: background-color 0.3s ease; margin-top: 10px; } button:hover { background-color: #0f4680; }
</style>
</head>
<body>
    <h1>Iniciar Sesión</h1>
    <form action="/api/sessions/login" method="POST">
        <label for="email">Email:</label>
        <input type="email" name="email" required>
        <label for="password">Contraseña:</label>
        <input type="password" name="password" required>
        <button type="submit">Iniciar Sesión</button>
    </form>
</body>
</html>